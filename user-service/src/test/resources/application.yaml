spring:
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

app:
  keycloak:
    serverUrl: http://localhost:8090/auth
    realm: bloggingnetwork
    adminUsername: admin
    adminPassword: admin