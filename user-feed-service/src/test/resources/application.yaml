spring:
  cloud:
    config:
      enabled: false
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://keycloak/auth/realms/master

eureka:
  client:
    enabled: false